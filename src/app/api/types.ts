export interface FirestoreCustomer{
    sessionId?: string;
    name?: string;
    phoneNumber?: string;
}
export interface BusinessInfo {
    name?: string;
    phoneNumber?: string;
    timezone?: string;
}